{
  "name": "repolens-mcp",
  "description": "Local-first MCP server that incrementally indexes repositories into SQLite and exposes architecture reports, graph search, dependency-cycle checks, recommendations, change impact, dead-code candidates, graph snapshots, and ADR memory tools.",
  "command": "npx",
  "args": ["-y", "repolens-mcp", "mcp"],
  "env": {
    "REPOLENS_DB": ".repolens/memory.db"
  }
}
