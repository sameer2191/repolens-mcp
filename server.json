{
  "name": "repolens-mcp",
  "description": "Local-first MCP server that indexes repositories into SQLite and exposes architecture, graph search, change impact, dead-code candidates, graph snapshots, and ADR memory tools.",
  "command": "npx",
  "args": ["-y", "repolens-mcp", "mcp"],
  "env": {
    "REPOLENS_DB": ".repolens/memory.db"
  }
}
