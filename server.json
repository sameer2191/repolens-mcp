{
  "name": "repolens-mcp",
  "description": "Local-first MCP server that incrementally indexes repositories into SQLite and exposes architecture reports, graph search, cross-repo graphing, graph communities, semantic search, route-call links, read-only graph queries, source snippets, dependency-cycle checks, recommendations, change impact, dead-code candidates, graph snapshots, graph package exchange, and ADR memory tools.",
  "command": "npx",
  "args": ["-y", "repolens-mcp", "mcp"],
  "env": {
    "REPOLENS_DB": ".repolens/memory.db"
  }
}
